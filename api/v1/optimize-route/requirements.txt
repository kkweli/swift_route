# Lightweight Route Optimization for Vercel Serverless
# Using OSRM for routing (no database needed)
# Optimized for Vercel Hobby plan constraints:
# - No heavy ML libraries (PyTorch, TensorFlow)
# - Minimal dependencies for fast cold starts
# - Uses external OSRM API instead of local graph processing
requests>=2.31.0
python-dotenv>=1.0.0
google-generativeai>=0.3.0
